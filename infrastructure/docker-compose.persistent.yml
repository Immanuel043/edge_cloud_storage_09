version: '3.8'

# Override file for production with Elasticsearch persistence
# Use with: docker-compose -f docker-compose.yml -f docker-compose.persistent.yml up -d

services:
  elasticsearch:
    volumes:
      - elasticsearch_data:/usr/share/elasticsearch/data
